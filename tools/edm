#!/usr/bin/env bash
set -euo pipefail

PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONPATH="${PROJECT_ROOT}/third_party/everest-dev-environment/dependency_manager/src${PYTHONPATH:+:${PYTHONPATH}}"
exec python3 -c 'import edm_tool; edm_tool.main()' "$@"
